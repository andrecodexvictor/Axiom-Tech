{
  "name": "axiom-tech-workspace",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "check": "npm --prefix frontend run check",
    "dev:frontend": "npm --prefix frontend run dev",
    "start:api": "uvicorn app.main:app --reload --port 8000",
    "test:backend": "python -m pytest tests/backend -q"
  }
}
